{
  "buildCommand": "npx prisma generate && next build",
  "installCommand": "npm install",
  "functions": {
    "src/app/api/**/*.ts": {
      "runtime": "@vercel/node"
    }
  },
  "regions": ["iad1"]
}